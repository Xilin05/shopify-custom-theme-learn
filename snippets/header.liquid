<header>
  <h1>{{ shop.name }}</h1>
  <h1>Coding With Shining - HEADER</h1>

  <!-- Main menu -->
  <ul>
    {% for link in linklists['main-menu'].links -%}
      <li>
        {{ link.title | link_to: link.url }}
      </li>
    {%- endfor %}
  </ul>
</header>
